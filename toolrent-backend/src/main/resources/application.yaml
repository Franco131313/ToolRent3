spring:
  application:
    name: toolrent-backend
  datasource:
    url: jdbc:mysql://localhost:3306/toolrent_db
    username: root
    password: Mysql@2026

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080